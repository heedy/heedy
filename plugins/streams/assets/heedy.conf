plugin "streams" {
    version = "0.1.0"
    description = "Development version of heedy streams"

    frontend = "streams/main.mjs"

    exec "server" {
        cmd = ["./server"]
    }
}

source "stream" {
    backend = {
        "/data": "unix://streams.sock"
        "/actions": "unix://streams.sock"
        "/act": "unix://streams.sock"
    }

    meta = {
        "schema": {
            "type": "object",
            "default": {}
        },
        "actor": {
            "type": "boolean",
            "default": false
        }
        "required": ["schema","actor"]
    }
}