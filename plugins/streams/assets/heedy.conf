plugin "streams" {
    version = "0.1.0"
    description = "Testing version of heedy streams - you can use this plugin to try stream features as they are being developed."

    exec "server" {
        cmd = ["./streams"]
    }
}

source "stream" {
    routes = {
        "/data": "unix://streams.sock"
    }
}