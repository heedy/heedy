// NOTE: The plugin_manager plugin is NOT a normal plugin -
// it does not work in standalone mode, since 
plugin "registry" {
    version = "0.1.0"
    description = "Access to the main heedy plugin registry"

    frontend = "registry/main.mjs"
}