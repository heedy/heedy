preferences_schema = {
    "deez": null,
    "trees": {"type": "string"}
}

plugin "yomomma" {
    preferences_schema = {
        "yeet": {"type": "number"},
        "tree": {"type": "string"}
    }
}