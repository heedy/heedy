import React, {Component, PropTypes} from 'react';

export default function Loading() {
    return (
        <div style={{
            textAlign: "center",
            paddingTop: 200,
            paddingBottom: 100
        }}>
            <p>Loading...</p>
        </div>
    );
}
