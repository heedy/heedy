/*
Inputs gives custom components for inserting data into ConnectorDB.
These components are registered using addinput from ../datatypes.js.

This file is imported at the beginning of the app, and it imports
all of the available inputs, so that they can register themselves.
*/

// The default form to show
import './default';
