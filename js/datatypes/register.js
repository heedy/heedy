/*
This file registers all creators, inputs, and views - this is dones imply by importing the respective registries.

This file is imported at the beginning of the app, so that all visualizations can be initialized quickly.
*/

import './creators/registry';
import './inputs/registry';
import './views/registry';
